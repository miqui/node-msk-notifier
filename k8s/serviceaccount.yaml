apiVersion: v1
kind: ServiceAccount
metadata:
  name: node-msk-notifier
  namespace: notifications
  annotations:
    # Populated by Terraform output: aws_iam_role.irsa.arn
    # Update this value after running terraform apply for the first time.
    eks.amazonaws.com/role-arn: PLACEHOLDER_IRSA_ROLE_ARN
  labels:
    app.kubernetes.io/name: node-msk-notifier
    app.kubernetes.io/managed-by: terraform
